.login-logo
  %h1= t(:app_name)
.login-area
  %h2= t('.new_session')
  = simple_form_for @admin, url: backend_session_create_path do |form|
    = display_alert
    = form.input_field :email
    = form.input_field :password
    = form.submit t('.login'), class: 'btn btn-large', id: 'login'
